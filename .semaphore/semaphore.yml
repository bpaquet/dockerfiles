version: v1.0
name: Build image
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804
blocks:
  - name: bootstrap
    dependencies: []
    task:
      jobs:
        - name: ls
          commands:
            - ls
  - name: docker build
    dependencies:
      - bootstrap
    task:
      jobs:
        - name: docker build
          commands:
            - docker build -t foo elasticsearch-2.4.4/
